#! /bin/bash

yellow='#F1FF52'

case "$1" in
    --toggle)
        fcitx-remote -t
        ;;
    *)
        if [ "$(fcitx-remote)" = "1" ]; then
            echo ""
        else
            echo "%{F$yellow}"
        fi
        ;;
esac